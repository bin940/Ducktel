spring:
  datasource:
    url: jdbc:mysql://localhost:3306/duckhotel?useSSL=false&allowPublicKeyRetrieval=true
    username: duckadmin
    password: Ducktel123
    driver-class-name: com.mysql.cj.jdbc.Driver

logging:
  level:
    root: DEBUG
    org.springframework.web: DEBUG
    com.ducktelback: DEBUG
    sun.rmi.transport.tcp: warn
    sun.rmi.loader: warn
    javax.management.remote.rmi: warn